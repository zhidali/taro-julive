<image
  class="my-class {{className}} {{classImage}} {{(isLoadError && placeholder.length ==0) ? 'background-color' : ''}}"
  src="{{url.length>0?url:placeholder}}"
  binderror="binderror"
  mode="aspectFill"
  bindtap="didTapImage"
  data-index="{{index}}"
  lazy-load
/>
